// import { getHubMatches } from '@/services/faceit-api';
// import { event } from '@/utils';

// export default event('ready', async ({ log }, client) => {
//   try {
//     const matches = await getHubMatches();

//     for (const match of matches.items) {
//       handleNewMatch(match, interaction, client);
//     }
//   } catch (error) {
//     log('[Match Voice Channels Error]', error);
//     throw error;
//   }
// });
